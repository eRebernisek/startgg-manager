import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'startgg.manager.app',
  appName: 'startgg-manager',
  webDir: 'dist/startgg-manager/browser'
};

export default config;
